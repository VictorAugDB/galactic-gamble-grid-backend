export function balance(userId: string) {
  return `balance:${userId}`
}

export function bets(userId: string) {
  return `bets:${userId}`
}

export function tickets(userId: string) {
  return `tickets:${userId}`
}
